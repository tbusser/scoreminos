import { PlayerSummary } from '01-global/interface';

/* == EXPORTS =============================================================== */
export interface RemainingPoints {
	id: PlayerSummary['id'];
	points: number;
}
