import { PlayerSummary } from '01-global/interface/PlayerSummary';

/* == EXPORTS =============================================================== */
export interface RemainingPoints {
	id: PlayerSummary['id'];
	points: number;
}
