import { PlayerSummary } from './PlayerSummary';

/* == EXPORTS =============================================================== */
export interface PlayerPoints {
	id: PlayerSummary['id'];
	points: number;
}
