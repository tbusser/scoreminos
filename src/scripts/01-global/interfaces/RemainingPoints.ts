import { PlayerSummary } from '01-global/interfaces/PlayerSummary';

/* == EXPORTS =============================================================== */
export interface RemainingPoints {
	id: PlayerSummary['id'];
	points: number;
}
